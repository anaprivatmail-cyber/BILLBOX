# Netlify Functions (server-side) environment variables
# Copy to `netlify/functions/.env` for local tests OR set in Netlify → Site settings → Environment variables.
# Do NOT commit real secrets.

# Required (OCR + entitlements/quota)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Required (Google Vision). Provide the FULL JSON as a single line string.
# Either name works (code checks both):
GOOGLE_SERVICE_ACCOUNT_JSON=
# GOOGLE_APPLICATION_CREDENTIALS_JSON=

# Optional (AI help for non-payment-critical fields; amount is never AI-filled)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini

# Optional: explicitly disable AI even if OPENAI_API_KEY is set
# ENABLE_OCR_AI=false

# Optional (unrelated to OCR, but used by other functions)
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
